The stbtel creates a file that has two time records having the time step 0.

For serial computations, all is OK. But for parallel computations this poses a problem when GRETEL tries to decompose the mesh.